<html>
    <head>
        <title>Users</title>
    </head>
    <body>
        <h1>Users</h1>
        {% for user in users %}
            <div>
                {{ user.name }}
            </div>
        {% endfor %}
    </body>
</html>